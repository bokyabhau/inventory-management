{
  "name": "inventory-manager",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "client:install":"cd client && yarn && cd ..",
    "server:install":"cd server && yarn && cd ..",
    "client:build":"cd client && yarn build && cd ..",
    "server:build":"cd server && yarn build && cd ..",
    "prebuild": "yarn client:install && yarn server:install",
    "build": "yarn server:build && yarn client:build",
    "deploy": "cd server && yarn start:prod"
  }
}
